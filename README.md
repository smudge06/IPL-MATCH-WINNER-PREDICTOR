# IPL-MATCH-WINNER-PREDICTOR
This project is a Machine Learning-based web application that predicts the winner of an IPL match based on key match features.  The model is trained using historical IPL data and uses a Random Forest Classifier to make predictions.
